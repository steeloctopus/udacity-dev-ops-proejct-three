pipeline {
    agent any
        stages {
            stage('Build') {
            steps{
            sh 'This is the first step of the build'
            sh 'Finish Build'
             }
            }
        }
}