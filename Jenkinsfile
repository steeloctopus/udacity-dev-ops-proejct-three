pipeline {
    agent any
        stages {
            stage('Build') {
            steps{
            sh 'echo "this is the frist step of the build"'
            sh "'"
             }
            }
        }
}